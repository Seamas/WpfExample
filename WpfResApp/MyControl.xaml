<UserControl x:Class="WpfResApp.MyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfResApp"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <sys:String x:Key="cStr">控件内部的内容</sys:String>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="{StaticResource ResourceKey=aStr}" Margin="5"/>
        <TextBlock x:Name="tb2"
            Margin="5"/>
        <TextBlock Text="{StaticResource ResourceKey=cStr}" Margin="5"/>
    </StackPanel>
</UserControl>
